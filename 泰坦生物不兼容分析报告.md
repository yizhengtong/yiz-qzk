# 泰坦生物（TheTitansNeo）不兼容分析报告

## 概述

本模组通过 ASM Agent 字节码改写 `LivingEntity.getHealth()` 并引入 DELTA 系统来修改实体健康值。然而，**泰坦生物（EntityTitan）使用了一套完全独立的健康系统**，导致我们的伤害系统对其完全无效。

---

## 三大根本原因

### 原因①：EntityTitan 重写了 getHealth()（Java 方法覆盖）

```
EntityTitan.getHealth() → return getTitanHealth()
```

我们的 ASM LivingHealthTransformer 改写的是 `LivingEntity.getHealth()` 的字节码。但 EntityTitan **在 Java 层面重写了此方法**，JVM 调用时直接走 EntityTitan 的方法，**根本不会执行 LivingEntity 中被 ASM 改写过的方法**。

**关键点**：ASM 作用于字节码，而 Java 方法覆盖在 JVM 调度层面就拦截了调用。

### 原因②：EntityTitan 使用独立的 TITAN_HEALTH DataParameter

```
IEntityAnimatedHealth.getTitanHealth() → 从 SynchedEntityData 读取 TITAN_HEALTH
IEntityAnimatedHealth.setTitanHealth() → 写入 SynchedEntityData
```

泰坦的血量存储在自定义的 `TITAN_HEALTH` DataParameter 中，完全独立于 vanilla 的 `DATA_HEALTH_ID`。我们的 DELTA 系统修改的是 `DATA_HEALTH_ID` 通道，对泰坦的 `TITAN_HEALTH` 毫无影响。

### 原因③：泰坦伤害管道独立且严格

`hurt()` 方法在将伤害转发到 `baseHurt()` 之前，有严苛的限制：

| 限制类型 | 具体规则 |
|---------|---------|
| 伤害上限 | 非泰坦来源单次伤害 ≤ 1000 |
| 伤害下限 | 伤害 < 20 → 直接 return false（无视伤害） |
| 伤害类型黑名单 | 20+ 种伤害类型被拦截：MAGIC, EXPLOSION, FIRE, LAVA, DROWN, FALL, WITHER 等 |
| 玩家攻击 | 必须通过 `canBeHurtByPlayer()` 检查 |
| 无敌帧 | EntityTitan 有自己的 INVULTIME 无敌计时器 |

即使我们的伤害通过条件检查，`baseHurt()` 也是应用到 `TITAN_HEALTH`，而非 vanilla health。

---

## 叠加效应

```
我们的伤害管道：
  LivingIncomingDamageEvent → YizModQZKAPI.damage() → EntityASMUtil.addDelta() → getHealth() 返回 min(realHP, maxHP + delta)
                                                                    ↓
                                                          EntityTitan 完全绕过

EntityTitan 管道：
  EntityTitan.hurt() → baseHurt() → 应用 TITAN_HEALTH
  EntityTitan.tick() → setHealth(getTitanHealth())  ← 单向同步，覆盖 vanilla health
```

我们的 DELTA 系统修改的数据在 tick() 中被 `setHealth(getTitanHealth())` 抹除，因为 titan 每次 tick 都会用 TITAN_HEALTH 覆盖 vanilla health。

---

## 无法用当前架构解决

| 方法 | 可行性 | 原因 |
|------|-------|------|
| ASM 改写 EntityTitan.getHealth() | ❌ | 需加载时检测所有 EntityTitan 子类，维护成本高 |
| Mixin @Overwrite EntityTitan.getHealth() | ❌ | EntityTitan 在第三方 JAR 中，Mixin 无法定位 |
| `hurt()` 事件监听 | ⚠️ 部分可行 | 可监听 LivingIncomingDamageEvent，但 EntityTitan.hurt() 有自己的前置过滤 |
| 提交 PR 给 TitansMod 添加 API | ✅ | 让泰坦模组提供伤害注入接口 |

## 建议

1. 短期：报告此问题为已知限制
2. 长期：可开发针对性的 Mixin 兼容层，通过 Mixin 在 EntityTitan.hurt() 中注入钩子（如果 TitansMod 被纳入 Mixin 插件系统）
3. 替代方案：通过 `NeoForge.EVENT_BUS` 监听 `LivingIncomingDamageEvent`，在事件中对攻击者应用额外效果（如中毒、虚弱），间接对抗泰坦
