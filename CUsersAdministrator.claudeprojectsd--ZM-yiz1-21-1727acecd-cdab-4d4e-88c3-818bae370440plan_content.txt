Now I have a complete understanding of the codebase. Let me present the detailed implementation plan.\n\n---\n\n## Detailed Implementation Plan: Health Modification System\n\n### Pre-Plan Assessment: Current State\n\nAfter thorough codebase exploration, here is the accurate ground truth.\n\n**Working components:**\n- `LivingEntityMixin` -- Modifies `getHealth`/`isAlive`/`isDeadOrDying` at RETURN using memory-map delta via `EntityASMUtil`. Tick decay, die cleanup, and `HealthModificationScheduler.tick()` are functional.\n- `EntityASMUtil` -- Central hub for delta management. Uses `Object2FloatOpenHashMap<UUID>`. The `specialGetHealth`/`specialIsAlive`/`specialIsDeadOrDying` methods match the signatures expected by `LivingHealthTransformer` in the agent jar.\n- `HealthValueCalculator`, `HealthApplier`, `HealthModificationManager`, `MultiModifierAggregator`, `HealthModifier`, `HealthModificationScheduler` -- Complete health modification pipeline.\n- `HealBanHandler` -- NeoForge event-based (LivingHealEvent + LivingDamageEvent.Post). Correct logic.\n- `AttributeDamageHandler` -- LivingIncomingDamageEvent-based attribute damage.\n- `HealthAttributeHandler` -- Subscribes to `HealthModificationEvent`, adds flat/percent damage modifiers from attributes.\n- `HealthCommand` -- /yiz agent/lkgd/lkbfb/jl commands.\n- `AttackInterceptorMixin` -- Player.attack HEAD injection, DIRECT_HEALTH_MOD damage tag handling.\n\n**Broken/missing components:**\n1. ASM Agent always fails -- `jdk.attach` module unavailabile in NeoForge dev runtime.\n2. Delta NOT persisted (memory map only, no NBT save/load).\n3. Delta NOT synced between client/server (no `EntityDataAccessor`).\n4. `PlayerMixin` does not exist.\n5. `SyncedEntityDataMixin` does not exist.\n6. `EntityActuallyHurt` class does not exist.\n7. `HealthCommand` `/yiz agent` always reports success regardless of actual agent state.\n\n---\n\n### Step 1: Fix ASM Agent Loading\n\n**Objective:** Make the ASM agent actually load in dev environment by providing the `jdk.attach` module, and add graceful fallback when it cannot load.\n\n**Rationale:** The ASM layer is needed for intercepting `getHealth`/`isAlive`/`isDeadOrDying` on third-party mod entities whose classes the Mixin cannot target. Without it, the system only works on vanilla Minecraft entities.\n\n**Files to modify:**\n\n1. **`D:\\ZM\\yiz1.21.1\\build.gradle`**\n\n   Add JVM arguments in the `configureEach` block (around line 44-62) to expose `jdk.attach` and open required packages for reflection:\n\n   ```groovy\n   runs {\n       configureEach {\n           // ... existing systemProperty lines ...\n\n           // === ASM Agent support ===\n           // jdk.attach is needed for VirtualMachine.attach() (dynamic agent loading)\n           jvmArgument \
