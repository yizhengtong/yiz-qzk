# GUI 搭建技术守则

> 搭建 Minecraft GUI 时必须遵守的技术规则。
> 遇到问题先翻这里，不要猜。

---

## 1. 原件保护守则

`GUI/description/` 目录下的所有图片文件为原本样式（原件），严格执行：

1. 禁止移动、删除、重命名原件
2. 禁止修改原件的像素内容
3. 需要使用时，**必须复制原件到工作目录**，在工作副本上进行裁剪、拼接等操作

---

## 2. 坐标系统 & 1px 偏移

### 现象

`AbstractContainerScreen.isHovering()` 判断点击命中时，起点是 `(slot.x - 1, slot.y - 1)` 而不是 `(slot.x, slot.y)`。

### 根本原因

```java
// AbstractContainerScreen 第 637-650 行
protected boolean isHovering(int x, int y, int width, int height, double mouseX, double mouseY) {
    int i = this.leftPos;
    int j = this.topPos;
    mouseX -= (double)i;
    mouseY -= (double)j;
    return mouseX >= (double)(x - 1)        // 命中盒起点 = slot.x - 1
        && mouseX < (double)(x + width + 1)
        && mouseY >= (double)(y - 1)        // 命中盒起点 = slot.y - 1
        && mouseY < (double)(y + height + 1);
}
```

### 关键公式

```
槽位背景纹理绘制起点 = leftPos + slot.x - 1     ← 永远用这个公式
槽位背景纹理绘制终点 = leftPos + slot.x + 17    (= leftPos + slot.x - 1 + 18)
物品渲染起点        = leftPos + slot.x           (物品 16×16，在槽位纹理内部居中)
命中检测范围        = (slot.x-1, slot.y-1) ~ (slot.x+16, slot.y+16)
```

**核心原则**：所有槽位背景纹理必须从 `(slot.x - 1, slot.y - 1)` 开始绘制，才能与 `isHovering` 命中盒完美重叠。

### 代码体现

```java
// slot_default：绘制在 (slot.x-1, slot.y-1)，偏移 -1px 对齐命中区
drawTex(graphics, TEX_SLOT,
    x + BORDER + col * SLOT - 1,
    chestY + row * SLOT - 1,
    18, 18);

// player_slots_9x4：整块纹理偏移 -1px
drawTex(graphics, TEX_SLOTS, x + BORDER - 1, playerY - 1, 162, 76);
```

---

## 3. 窗口尺寸计算公式

```
SLOT   = 18     // 槽位间距
BORDER = 5      // 边框厚度

内容区宽度 = COLS × SLOT
总窗口宽度 = BORDER × 2 + 内容区宽度

容器区高度 = ROWS × SLOT
分隔条     = 14
玩家背包   = 76

总窗口高度 = BORDER + 容器区高度 + 分隔条 + 玩家背包 + BORDER
```

标准 9 列容器示例（以 3 行箱子为例）：

```
总窗口 172×154：
┌── TL ── top_edge ── TR ──┐  ← 5px
│  ░░ ░░ ░░ ░░ ░░ ░░ ░░ ░░ ░░  │  ← 3行 × 18px = 54px  容器槽区
├───────── split_bar ────────┤  ← 14px
│       player_slots_9x4      │  ← 76px  玩家背包
└── BL ── bottom_edge ── BR ─┘  ← 5px
```

---

## 4. 大容器变列算法

当容器格数超过 27（3×9）时，窗口会变得过长。此时不再固定 9 列，而是动态计算列数。

### 算法

```
输入：总格数 N

N ≤ 27 → 固定 9 列（标准箱子风格）

N > 27 → 在 [9, 16] 列范围内寻找最优列数：
  for cols in 9..16:
      rows = ceil(N / cols)
      ratio = max(cols, rows) / min(cols, rows)
  选 ratio 最接近 1 的 (cols, rows) 组合

限制：cols < 9 会与 18px 槽位尺寸不兼容
      cols > 16 会使窗口过宽，超出常见屏幕
```

### 关键值对照

| 格数 | 最优方案 | 窗口尺寸 |
|------|---------|---------|
| 27 | 9×3 | 172×154 |
| 54 | 9×6 | 172×208 |
| 72 | 9×8 | 172×262 |
| 100 | **10×10** | 190×274 |
| 128 | **12×11** | 226×298 |
| 200 | **14×15** | 262×352 |
| 300 | **16×19** | 298×436 |

### 玩家背包居中规则

当容器列数 > 9 时，底部玩家背包（固定 162px 宽）在内容区中水平居中：

```
invOffset = (COLS - 9) × SLOT / 2
背包绘制起点 = leftPos + BORDER + invOffset - 1
```

```java
// 容器槽位：从 BORDER 开始，占满内容区宽度
addSlot(new Slot(chestInv, index,
    BORDER + col * SLOT, BORDER + row * SLOT));

// 玩家背包：在内容区中居中
int invOffset = (COLS - 9) * SLOT / 2;
addSlot(new Slot(playerInv, slotIndex,
    BORDER + invOffset + col * SLOT, PLAYER_INV_Y + row * SLOT));
```

---

## 5. ResourceLocation 路径规则

**`ResourceLocation` 的路径必须包含 `.png` 扩展名**，否则游戏找不到纹理文件，显示紫黑色丢失纹理。

```java
// ❌ 错误：缺少 .png → 紫黑色
"textures/gui/container/slot_default"

// ✅ 正确：包含 .png → 正常显示
"textures/gui/container/slot_default.png"
```

完整用法：

```java
private static final ResourceLocation TEX_SLOT = ResourceLocation.fromNamespaceAndPath(
    "yizmodqzk", "textures/gui/container/slot_default.png");
```

文件位于 `src/main/resources/assets/yizmodqzk/textures/gui/container/slot_default.png`（不是项目根目录的 `assets/`）。

---

## 6. 容器点击修复（containerId）

客户端直接 `mc.setScreen()` 打开的容器使用 `containerId = -1`，服务端不认这个 ID，所有槽位点击都会被丢弃。

**修复方式**：在单机模式下通过集成服务端打开容器，获得有效 containerId：

```java
if (server != null) {
    ServerPlayer serverPlayer = server.getPlayerList().getPlayer(mc.player.getUUID());
    if (serverPlayer != null) {
        serverPlayer.openMenu(new SimpleMenuProvider(
            (id, playerInv, p) -> new TestChestMenu(id, playerInv),
            Component.literal("Test Chest")
        ));
    }
}
```

这会让服务端分配真实 containerId、发送 `ClientboundOpenScreenPacket`，客户端收到后自动打开对应 Screen。此后所有槽位点击发往服务端处理，服务端同步结果回客户端。

---

## 7. Screen ↔ Menu 坐标对齐规则

**所有自定义槽位的 Menu 坐标必须和 Screen 绘制坐标严格一致**，不一致会导致 1px 偏移规则失效。

### 错误示例

```java
// Screen: 槽位纹理绘制在 y=45
private static final int SLOT_ROW_Y = 45;
g.blit(TEX_SLOT, x + BORDER + col * SLOT - 1, y + SLOT_ROW_Y - 1, 18, 18);

// Menu: 槽位坐标在 y=42  ❌ 差 3px
addSlot(new Slot(container, 0, 5 + 0 * 18, 42));
```

### 正确做法

```java
// Menu 直接使用 Screen 的布局常量
addSlot(new Slot(container, 0, 5 + 0 * 18, SLOT_ROW_Y));
```

### 玩家背包同理

```java
// Screen: PLAYER_Y = 92
g.blit(TEX_SLOTS, x + BORDER - 1, y + PLAYER_Y - 1, 162, 76);

// Menu: playerInvY 必须等于 PLAYER_Y
int playerInvY = PLAYER_Y; // 92
```

---

## 8. renderBg 勿重复调用

**不要同时 override `render()` 和 `renderBg()`**。`AbstractContainerScreen.render()` 内部已调用 `renderBg()`，如果 override `render()` 时又手动调用 `renderBg()`，会引发：

- 背景纹理重复绘制（性能浪费）
- `super.render()` 返回后才能渲染槽位物品和点击检测 → 被你的额外 `renderBg()` 挤占
- 物品渲染和命中检测错乱

### 错误示例

```java
// ❌ render() 里手动调 renderBg()，super.render() 里又调一次
@Override
public void render(GuiGraphics g, int mx, int my, float delta) {
    renderBg(g, delta, mx, my);           // 第 1 次
    super.render(g, mx, my, delta);       // 第 2 次（在 AbstractContainerScreen 里调）
    renderTooltip(g, mx, my);             // 第 2 次 tooltip
}
```

### 正确做法

只 override `renderBg()`，让原版 `render()` 负责调用它：

```java
// ✅ 只 override renderBg，render() 不碰
@Override
protected void renderBg(GuiGraphics g, float delta, int mx, int my) {
    // 只画背景，其余交给原版
}
```

---

## 变更记录

| 日期 | 变更 |
|------|------|
| 2026-05-16 | 从原 `GUI纹理拆解计划表.md` 拆分出独立技术守则 |
| 2026-05-16 | 新增 §7 Screen↔Menu 坐标对齐规则 |
| 2026-05-16 | 新增 §8 renderBg 勿重复调用规则 |
