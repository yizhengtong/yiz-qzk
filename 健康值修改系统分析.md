# 健康值修改系统

三层拦截 + 一套独立状态机"，从 JVM 字节码层面到 Java 事件层面
---

## 一、设计理念：为什么要这么复杂？

原版 Minecraft 的健康系统很简单——`LivingEntity` 有一个 `DATA_HEALTH_ID` 的 `DataParameter<Float>`，`getHealth()` 读它，`setHealth()` 写它，`hurt()` 做减法


我们模组要实现的却是：

- **临时血量上限缩减**（类似"受伤上限"机制，随时间恢复）
- **自定义伤害类型**（无视盔甲、百分比伤害、真伤等）
- **禁止治疗**（效果）
- **锁血/无敌**（玩家不死模式）
- **死亡覆写**（某些条件下不该死就别死）
- 这些功能必须在**所有**模组生物（包括其他模组的实体）上生效

原版的 API 根本不够用，所以需要在ASM 在字节码层拦截关键方法。

---

## 二、如何"收集"健康值 —— 三层拦截架构

### 第一层：ASM 字节码注入（最底层，无差别覆盖）

入口是 `FantasyEndingMixinPlugin.java:103` 的 `LivingEntityCheckTransformer`，这是一个标准的 Java Agent `ClassFileTransformer`。

核心逻辑：**对所有继承 `LivingEntity` 的类**（包括其他模组的实体），在类加载时用 ASM 改写字节码。

负责在访问类的所有方法时，识别出三个关键方法：

1. **`getHealth()` → `GetHealthModifyMethodVisitor`**
   - 匹配规则非常宽泛：方法名含 `gethealth`、或以 `get` 开头以 `health`/`hp` 结尾、或 `getCombatProgress` 等
   - 在 `FRETURN` 指令前注入：`EntityASMUtil.special_getHealth(栈顶的float, this)`
   - 效果：**任何方法调用 `getHealth()`，返回值都会被这个静态方法接管**

2. **`isAlive()` → `IsAliveModifyMethodVisitor`**
   - 在 `IRETURN` 前注入：`EntityASMUtil.special_isAlive(原始返回值, this)`

3. **`isDeadOrDying()` → `IsDeadOrDyingMethodVisitor`**
   - 在 `IRETURN` 前注入：`EntityASMUtil.special_isDeadOrDying(原始返回值, this)`

4. **`StaticHealthManagerVisitor`** — 额外处理非 `LivingEntity` 自身类中对 `getHealth()`/`isAlive()`/`isDeadOrDying()` 的**静态调用**（`INVOKESTATIC`），同样重定向到 `EntityASMUtil.special_*` 方法。

> **为何需要 ASM 而不是 Mixin？** Mixin 只能混入你自己的模组目标类，无法强制其他模组的实体也走你的逻辑。ASM 在类加载层面改写字节码，对所有实体无差别生效。

### 第二层：Mixin 注入（数据定义层）

`LivingEntityMixin.java` 在 `LivingEntity` 上做了几件事：

- 在 `<clinit>` 中定义同步数据参数：
  - `FE_GET_HEALTH_DATA`（`EntityDataSerializers.FLOAT`）— 这是"健康增量"的通信管道
- 在 `defineSynchedData` 中初始化默认值（`0F`）
- 在 `readAdditionalSaveData` / `addAdditionalSaveData` 中持久化读写
- tick 处理：每 100 tick 将 delta 衰减向 0

`PlayerMixin.java` 处理玩家特有的无敌模式：
- 定义 `FE_INVULNERABLE_DATA`（`EntityDataSerializers.BOOLEAN`）
- 覆写 `getHealth()`，无敌时返回 `max(1, maxHealth)`
- 在 `hurt()` 头部取消伤害

### 第三层：SyncedEntityData 拦截（最上层）

`SyncEntityDataMixin.java` 的创造力极高——它混入 `SynchedEntityData.set()` 方法。

当实体带有 `BAN_HEALING` 效果时，任何对血量 `DataParameter` 的写入都被拦截：

```java
if ((float) p_276363_ > living.getHealth()) {
    // 治疗量 = (目标血量 - 当前血量) * (1.0 - (效果等级+1)*0.1)
    // 即 10%/级 的治疗削减，最高削减到 0
    p_276363_ = (T) (Object) (((float) p_276363_ - living.getHealth()) *
            Mth.clamp((1.0F - (amplifier + 1) * 0.1F), 0F, 1F)
            + living.getHealth());
}
```

注意这里区分了 `Float` 和 `Double` 类型的健康数据参数，因为有些模组的实体用 `Double` 存血量。

---

## 三、如何"确定"健康值 —— 健康增量（Health Delta）机制

这是整个系统的核心创新。

### 概念

每个 `LivingEntity` 有一个额外的 `EntityDataAccessor<Float>` 叫 `FE_GET_HEALTH_DATA`，存的是一个**偏移量（delta）**。

真正的"有效血量上限"不再是 `getMaxHealth()`，而是：

```
有效血量上限 = getMaxHealth() + delta
有效当前血量 = min(getHealth(), getMaxHealth() + delta)
```

### 核心实现：`EntityASMUtil.java`

```java
// ASM 注入点，每次 getHealth() 被调用时执行
public static float special_getHealth(float health, Object entity) {
    if (((LivingEntityEC)living).uom$livingECData().isDead)
        return 0.0F;                      // 标记为死亡 → 返回 0

    if (living instanceof Player player) {
        if (PlayerInvulnerableEntityData.isInvul(player))
            return Math.max(1.0F, player.getMaxHealth());  // 无敌 → 锁在 maxHealth
    }

    float delta = EntityASMUtil.getHealthDelta(living);
    return Math.min(health, living.getMaxHealth() + delta); // 核心：截断血量
}
```

这里的 `Math.min(health, maxHealth + delta)` 是关键——它保证：
- **delta 为负数时**（比如 -20）：血量被截断为 `maxHealth - 20`，即血量上限被压低
- **delta 为正数时**：理论上允许血量超过 maxHealth（虽然原版 `setHealth` 会限制在 maxHealth 内）

### Delta 的衰减：`LivingEntityMixin.java`

```java
if (this.tickCount % 100 == 0 && !isDeadOrDying()) {
    float delta = EntityASMUtil.getHealthDelta(livingEntity);
    if (delta <= -1)
        EntityASMUtil.addDelta(livingEntity, 1.0F);   // 负 delta 向 0 恢复
    else if (delta >= 1)
        EntityASMUtil.addDelta(livingEntity, -1.0F);   // 正 delta 向 0 恢复
    else if (delta < 1)
        EntityASMUtil.setHealthDelta(livingEntity, 0F); // 归零
}
```

每秒（100 tick）朝 0 方向调整 1 点。这实现了"受伤上限随时间的自然恢复"。

### Delta 的设置权限控制：`EntityASMUtil.java`

```java
public static void setHealthDelta(LivingEntity player, float value) {
    if (value <= 0 && !player.level().isClientSide) {
        // 仅在服务端设置非正值
        player.getEntityData().set(FE_GET_HEALTH_DATA, value);
    }
}
```

这个限制很关键：delta 只能设为 ≤0 值，且只能在服务端设置。这避免了客户端滥用 delta 系统。

---

## 四、如何"修改"和"锁定"健康值

### 1. 伤害即截断（自定义伤害管道）

`EntityActuallyHurt.java` 提供了完整的替代伤害系统。

**普通伤害** `actuallyHurt0()`：
```java
public void actuallyHurt0(DamageSource source, float amount, boolean special) {
    float finalHealth = Math.min(entity.getHealth() - amount, entity.getMaxHealth());
    entity.setHealth(finalHealth);
    if (special) catchSetTrueHealth(entity, finalHealth);  // 直接写底层数据
}
```

**Delta 伤害** `actuallyHurt0ForDelta()`：
```java
public void actuallyHurt0ForDelta(DamageSource source, float amount, boolean special) {
    float currentHealth = Math.min(entity.getHealth() - amount, entity.getMaxHealth());
    EntityASMUtil.addDelta(entity, -(entity.getHealth() - currentHealth));
    // 不是直接扣血，而是降低 delta！
}
```

两种伤害模式：
- `actuallyHurt()`：直接扣血，受 `special_getHealth` 截断
- `actuallyHurtForDelta()`：扣的是 delta（即压缩血量上限），然后 `special_getHealth` 在返回时自动截断

### 2. 强制写底层数据

`catchSetTrueHealth()` 使用 `VarHandle` 反射直接设置 `SynchedEntityData.DataItem.value`，完全绕过 `LivingEntity.setHealth()` 的所有逻辑：

```java
public static void catchSetTrueHealth(LivingEntity living, float value) {
    checkAndSave(living);  // 先确定该实体用哪个 DataParameter 存血量
    IndexAndType indexAndType = getIndexAndType(living);
    // 直接找到 itemsById 中对应的 DataItem，用 VarHandle 写 value 字段
    set(accessor, (EntityDataAccessor<Float>) accessor.itemsById().get(indexAndType.index()), value);
}
```

### 3. 血量 DataParameter 的自动发现（checkAndSave）

`EntityActuallyHurt.java` 的 `checkAndSave0()` 方法堪称艺术品——它通过**反射+试探**的方式，自动发现每个实体类用哪个 `DataParameter` 存储血量：

```java
// 策略 1：用 isHealthField 谓词过滤静态 EntityDataAccessor 字段
isHealthField = field -> {
    String name = field.getName().toUpperCase().replace("_", "");
    return name.contains("HEALTH")
        && !name.contains("MAXHEALTH")  // 排除最大血量
        && field.getType() == EntityDataAccessor.class
        && Modifier.isStatic(field.getModifiers());
};

// 策略 2：试探写入法
living.entityData.set(LivingEntity.DATA_HEALTH_ID, health - 0.1F);
dataHealth = getValue(living, LivingEntity.DATA_HEALTH_ID);
if (Objects.equals(dataHealth, living.getHealth())) {
    // 确认这个 DataParameter 确实控制血量
}
```

这个方法使模组可以兼容任何其他模组的实体——即使它们不使用原版的 `DATA_HEALTH_ID`。

### 4. 锁血（玩家无敌模式）

`PlayerInvulnerableEntityData.java` 实现了一个开关式无敌：

- 开启时：`getHealth()` 恒返回 `max(1, maxHealth)`（PlayerMixin）
- `hurt()` 直接被 cancel（PlayerMixin）
- 关闭时：恢复真正血量、清除 delta

被 ASM 注入的 `special_getHealth` 中也有无敌判断（EntityASMUtil）。

### 5. 死亡覆写 isDead 标志

`LivingEntityExpandedContext.java` 中有一个 `isDead` 布尔字段。当它被设置时：

- `special_getHealth` 返回 0.0F
- `special_isAlive` 返回 false
- `special_isDeadOrDying` 返回 true

这允许任何代码逻辑在"不应该活"的时候强制实体判定为死亡，即使血量还不为 0。这在 `EntityActuallyHurt.die()` 和 FantasyEndingArmor 的死亡保护中都有使用。

### 6. Health Reverse（逆命/回溯）

这是结合了 delta 和直接治疗的复杂机制：

`HealthReverseSpell.java`：
```java
// 减少 delta（恢复血量上限）
actuallyHurtForDelta(..., delta - (delta * percent), true);
// 给予 HEALTH_REVERSE 效果
entity.addEffect(new MobEffectInstance(ModMobEffects.HEALTH_REVERSE.get(), ...));
```

`HealthReverseMobEffect.java`：
- 死亡时：取消死亡，设血量为 `maxHealth * 0.05 + 3`
- 每 tick：治疗 `level + 1` 点，清除中毒/凋零/虚弱/缓慢/黑暗/饥饿

这通过 `LivingDeathEvent` 的 `setCanceled(true)` 实现——也是"锁血"的一种形式：不死。

---

## 五、架构全景图

```
┌─────────────────────────────────────────────────────────┐
│                    第三方模组                            │
│  调用 entity.getHealth() / entity.isAlive()             │
└──────────────────┬──────────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────────┐
│  Layer 1: ASM 字节码改写 (ClassFileTransformer)         │
│                                                         │
│  SoftGetHealthClassVisitor                              │
│  ├── GetHealthModifyMethodVisitor                       │
│  │   └─ FRETURN → EntityASMUtil.special_getHealth       │
│  ├── IsAliveModifyMethodVisitor                         │
│  │   └─ IRETURN → EntityASMUtil.special_isAlive         │
│  ├── IsDeadOrDyingMethodVisitor                         │
│  │   └─ IRETURN → EntityASMUtil.special_isDeadOrDying   │
│  └── StaticHealthManagerVisitor                         │
│      └─ INVOKESTATIC → 同上重定向                       │
└──────────────────┬──────────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────────┐
│  Layer 2: EntityASMUtil 决策中心                        │
│                                                         │
│  special_getHealth(health, entity):                     │
│  ├─ isDead? → 0.0                                      │
│  ├─ Player 无敌? → max(1, maxHealth)                    │
│  └─ 正常 → min(health, maxHealth + delta)               │
│                                                         │
│  special_isAlive/isDeadOrDying:                         │
│  ├─ isDead 标志? → 强制覆写                             │
│  └─ delta ≠ 0? → 用 health>0 判断                      │
└──────────────────┬──────────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────────┐
│  Layer 3: Mixin + Event 层                              │
│                                                         │
│  ├─ SyncEntityDataMixin: BAN_HEALING 削减治疗量         │
│  ├─ PlayerMixin: 无敌模式 getHealth/hurt 覆写           │
│  ├─ LivingEntityMixin: delta 衰减、MEB 攻击、数据持久化  │
│  └─ EntityActuallyHurt: 完整的自定义伤害管道             │
│      ├─ actuallyHurt(): 直接扣血(受 delta 截断)          │
│      ├─ actuallyHurtForDelta(): 降低血量上限             │
│      ├─ catchSetTrueHealth(): 暴力写底层数据参数         │
│      └─ checkAndSave(): 自动发现实体的血量 DataParameter │
└─────────────────────────────────────────────────────────┘
```

---

## 六、关键设计洞察

1. **ASM 的宽匹配策略**：`GetHealthModifyMethodVisitor.is()` 匹配 `gethealth`、`getHealth`、`gethp`、`getCombatProgress` 等几乎所有合理命名方案，确保连那些自己实现了 `getHealth()` 的模组实体也能被拦截。

2. **Delta 的双向衰减**：每 100 tick 朝 0 移动 1 点，使"受伤上限降低"成为一个有恢复期的临时状态，而不是永久 debuff。

3. **DataParameter 自动发现**：通过 `checkAndSave0()` 的反射试探，兼容其他模组的自定义血量存储方式——这是整个系统能在其他模组实体上正常工作的关键。

4. **三层锁血机制**：无敌模式（上层）+ delta 截断（中层）+ isDead 标志（底层），三层互为备份。

5. **`SynchedEntityData.set()` Mixin 拦截**：这是阻止第三方模组治疗效果的最彻底手段——在数据写入层面直接削减治疗量，**任何调用 `setHealth()` 或 `heal()` 的代码都无法绕过**，因为最终都会走到 `SynchedEntityData.set()`。
